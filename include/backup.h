/* backup.h - redundant filesystem snapshots ("backups for backups"). */
#ifndef HISOKA_BACKUP_H
#define HISOKA_BACKUP_H
void backup_run(void);   /* the Backups app: make / restore / delete snapshots */
#endif
