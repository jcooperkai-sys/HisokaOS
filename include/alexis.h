/* alexis.h - Alexis, the HisokaOS assistant. The UI is native; the model runs on the
 * host (Ollama) and is streamed in over the network, the same way the browser works. */
#ifndef HISOKA_ALEXIS_H
#define HISOKA_ALEXIS_H
void alexis_run(void);
#endif
