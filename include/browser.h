/* browser.h - the streamed Chromium browser. Fetches a host-rendered page image
 * and paints it in graphics mode. */
#ifndef HISOKA_BROWSER_H
#define HISOKA_BROWSER_H
void browser_run(const char *url);
#endif
